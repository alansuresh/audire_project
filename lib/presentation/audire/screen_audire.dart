import 'package:flutter/material.dart';

class Screenaudire extends StatelessWidget {
  const Screenaudire({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Audire'),
    );
  }
}
